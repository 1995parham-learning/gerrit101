{
  "name": "marp-presentation",
  "version": "1.0.0",
  "description": "Marp presentation slides with GitHub Pages deployment",
  "scripts": {
    "build": "marp slides/ -o dist/ --html --theme-set themes/",
    "build:pdf": "marp slides/ -o dist/ --allow-local-files --pdf --theme-set themes/",
    "watch": "marp -w slides/ -o dist/ --html --theme-set themes/",
    "serve": "marp -s slides/ --html --theme-set themes/"
  },
  "keywords": [
    "marp",
    "presentation",
    "slides",
    "markdown"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@marp-team/marp-cli": "^4.2.3"
  }
}
